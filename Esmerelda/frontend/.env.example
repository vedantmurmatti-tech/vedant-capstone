# Copy to .env and fill in real values. .env is gitignored.

# Backend API base URL. Defaults to http://localhost:8000 in code
# (src/lib/api.ts) if unset, so local dev works without this file.
# Set to the deployed backend's real URL in production (not known yet —
# left blank here deliberately, per BUILD_LOG.md Steps 6 & 7).
VITE_API_BASE_URL=http://localhost:8000
