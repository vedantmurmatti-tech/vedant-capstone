venv/
__pycache__/
*.pyc
.env
tests/

# Local-only, interactive Moodle/Playwright automation — not part of the
# deployed API. moodle/sync_service.py (used by POST /api/sync/moodle) is
# NOT excluded — the rest of moodle/ (the manual-login tools) is.
moodle/browser.py
moodle/document_downloader.py
moodle/browser_profile/
moodle/__pycache__/
content_radar.py
course_radar.py
submission_radar.py
voice.py
browser_profile/
state/

# Local persistent data — not baked into the image; production data
# lives under ESMERELDA_DATA_DIR (see storage/paths.py) instead.
storage/esmerelda.db
storage/documents/
